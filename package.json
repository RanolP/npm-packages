{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "changeset": "changeset",
    "ci:publish": "pnpm publish -r --provenance --access public && changeset tag",
    "ci:version": "changeset version"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "turbo": "^2.5.3"
  }
}
